# LLM narrative generation (optional — falls back to a deterministic template if unset)
LLM_PROVIDER=none
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
LLM_BASE_URL=https://api.openai.com/v1

# Backend
BACKEND_CORS_ORIGINS=http://localhost:5173

# RAG
CHROMA_DIR=data/outputs/chroma
